# This project uses a Random Forest Classifier. 
### A report has been attached that goes into detail about the project.
#### In case the site is inactive ( streamlit deactivates a website if inacitivty for >5 days), you can install all the files , change directory to this folder and run requirements.txt to install all required libraries. Henceforth, you can run ' streamlit run app.py ' to run the app
